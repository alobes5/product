# product

Simple project with:
* flyway
* custom page error
* custom health endpoint
* custom App config
* actuator

## How to run:

```
./mvnw clean install
./mvnw clean package
./mvnw test
./mvnw compile
./mvnw spring-boot:run
```
